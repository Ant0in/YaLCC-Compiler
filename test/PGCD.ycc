!! Computation of PGCD for testing purpose !!

Prog PGCD Is

Input(a);
Input(b);
c = 0;

While {b <= a} Do
    a = a - b;  $ part 1
End;

While {a < b} Do
    b = b - a;  $ part 2
End;

c = a;
Print(c);  $ pgcd

End